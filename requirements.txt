# The StageFlow core — it is the core that executes the pipelines. A sibling
# ../stageflow checkout works too: serve.py falls back to it when the package
# is not installed.
stageflow-framework>=0.9

# Only the two stages in llm_stages.py need it. Without it the rest of the bot
# still works: the full pipeline falls back to keyword rules.
openai
